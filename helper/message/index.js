const auth = require('./auth.message');
const user = require('./user.message');
const access = require('./access.message');
const category = require('./category.message');
const product = require('./product.message');

module.exports = {
    auth,
    user,
    access,
    category,
    product,
}
